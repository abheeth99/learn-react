import React from 'react';
import './App.css';
import { useLocalStore, useObserver } from 'mobx-react-lite';

function App() {
  return (
      <main>
        box-master
      </main>
  );
}

export default App;
